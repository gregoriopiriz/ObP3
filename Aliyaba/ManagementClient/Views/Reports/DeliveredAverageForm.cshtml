@using (Html.BeginForm("DeliveredAverage", "Reports"))
{
    <div class="d-flex flex-column">
        <div class="form-group w-25">
            <label>Fecha Inicio:</label>
            <input type="date" name="Date1" class="form-control bg-dark shadow text-light" />
        </div>
        <div class="form-group w-25">
            <label>Fecha Fin:</label>
            <input type="date" name="Date2" class="form-control bg-dark shadow text-light" />
        </div>
        <div class="form-group">
            <button type="submit" class="btn btn-outline-info">
                Buscar
            </button>
        </div>
    </div>
}